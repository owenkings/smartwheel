ekf_filter_node:
  ros__parameters:
    frequency: 30.0
    sensor_timeout: 0.2
    two_d_mode: true
    publish_tf: false
    world_frame: odom
    odom_frame: odom
    base_link_frame: base_link
    odom0: /wheel/odom
    odom0_config: [false, false, false, false, false, false, true, true, false, false, false, true, false, false, false]
    # H30 IMU is intentionally absent here. It is already consumed by FAST-LIO2
    # in lio_primary mode and must not be fused twice.

