ekf_filter_node:
  ros__parameters:
    frequency: 30.0
    sensor_timeout: 0.2
    two_d_mode: true
    publish_tf: false
    world_frame: odom
    odom_frame: odom
    base_link_frame: base_link
    odom0: /wheel/odom
    odom0_config: [false, false, false, false, false, false, true, true, false, false, false, true, false, false, false]
    imu0: /imu/data_raw
    imu0_config: [false, false, false, false, false, true, false, false, false, false, false, true, false, false, false]
    imu0_remove_gravitational_acceleration: true
    # This configuration is launched only in wheel_imu_fallback mode. The
    # lio_primary path never feeds H30 into robot_localization a second time.
