# RETIRED: DO NOT APPLY.
#
# This historical patch changed LASER_POINT_COV from 0.001 to 100.0 and can
# reintroduce the stationary-divergence fault.  It is deliberately retained as
# a non-applicable tombstone so old automation fails safely instead of silently
# restoring the bad value.
#
# Use patches/apply_fastlio_patches.sh, which applies the complete pinned
# patches/fastlio_smartwheel_hardening.patch and verifies R=0.001 afterward.
