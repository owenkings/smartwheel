# 右雷达角度补偿记录（等待有效标定数据）

日期：2026-09-11。用户授权修正安装角度并记录。主树 `/home/nvidia/smartwheel`，基准 HEAD `103b569`；不修改 `smartwheel-rtab-old`。

## 当前角度：尚未替换

来源：`src/wheelchair_description/config/diagnostic_measured_layout.yaml` 的 right.rpy；上一轮还核对了正在运行的 robot_state_publisher 的实际 robot_description，两者一致。

```yaml
right:
  xyz: [0.45, -0.30, 0.735]
  rpy: [1.5806995918, 0.1564194958, 1.5715764604] # rad，含 SDK 轴到 ROS 轴变换
```

SDK 约定为 x 向左、y 向上、z 向前；ROS 车体为 x 向前、y 向左、z 向上。不能将这里约 90 度的 RPY 分量直接解释成安装侧倾。

去除标准 SDK 轴转换后，当前安装旋转矩阵为：

```text
 0.9999518623  -0.0007706092   0.0097815750
-0.0007626336   0.9877910930   0.1557824608
-0.0097822000  -0.1557824215   0.9877429553
```

其绕车体前向轴侧倾约 **-8.96°**（按此坐标约定左侧向下），前后俯仰约 **+0.56°**，水平朝向约 **-0.044°**。这些是旧配置的数学解释，**不是本轮实测得到的安装角**。该配置与用户“右雷达向右下倾”的文字描述存在方向冲突，但这不等于已经由地面点云证明应将角度反号；观察视角、物理左右和 SDK 轴必须统一。

## 本轮读取证据

1. 在线读取 `/xtm60/right/points`，8 秒内 0 帧；进程检查表明原建图程序已退出。没有自动重启真实设备，也没有发送运动/释放指令。
2. 只读检查已保存的 `maps/wheel_imu_20260911_145830_P4bF/raw`。取最初 60 帧，时间戳 1789109920.0872204 至 1789109925.9567587。对应轮速 vx/wz 与安全指令 vx/wz 最大绝对值均为 0；gyro 三轴最大绝对值约 0.00394/0.00353/0.00331 rad/s。
3. 按两个不重叠 30 帧窗口运行现有地面拟合质量检查，两组接受帧均为 **0/30**。示例平面内点比例约 15.3%/14.9%，P95 残差约 27.9/28.1 mm；平面距离示例约 0.533/0.686 m。低于既有 20% 内点门槛且超过 25 mm 残差门槛，不能据此确认是稳定真实地面。
4. 当前静止平地、裸露地面与安装固定条件尚待用户确认；本轮请求已发出。未伪造确认标志，未放宽阈值。

远端证据：`auto_test/right_angle_20260911/precheck.json`、`recorded_review.json`、`offline_floor_review.py`。该目录仅诊断产物，不是批准的标定文件。

## 修改状态与下一步

**本轮尚未写入新补偿角；现有 TF、IMU、左右雷达高度和正式标定门禁均未改动。**

需将轮椅静止放在平整地面，使右雷达前方有裸露地面且外壳不晃动。使用完整建图入口 `MOTION=false` 启动即可，不需要另开雷达单设备程序。确认环境后采集独立的估计/验证片段：只校正右雷达旋转，保留目前水平 yaw 和 xyz；单个地面不提供 yaw、水平平移、时间同步或 IMU 杆臂标定。

新角度通过地面支持范围、逐帧稳定性、独立验证残余倾角检查后，再记录新旧 RPY、物理侧倾/俯仰、差值、点云时间戳、残差与备份，并部署到明确 provisional 的诊断布局。正式六自由度标定/导航验收仍不因此通过。

静态 TF 修改后须重启建图并使用新会话；旧地图已经按旧角度累积，不能通过只改新 TF 宣称旧地图已修复。
